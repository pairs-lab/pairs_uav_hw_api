<?xml version="1.0"?>
<package format="2">

  <name>pairs_uav_hw_api</name>
  <version>1.0.0</version>
  <description>PAIRS UAV hardware API — plugin interface for flight controller communication</description>

  <maintainer email="alex@dsc-labs.io">PAIRS Team</maintainer>

  <license>BSD 3-Clause</license>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>cmake_modules</depend>
  <depend>geometry_msgs</depend>
  <depend>pairs_lib</depend>
  <depend>pairs_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>nodelet</depend>
  <depend>roscpp</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>

  <export>
    <nodelet plugin="${prefix}/nodelets.xml" />
    <pairs_uav_hw_api plugin="${prefix}/plugins.xml" />
  </export>

</package>
